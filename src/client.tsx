/**
 * @file dsh-oh-my-terminal 浏览器半入口（占位）
 * @description Cordis 插件浏览器半入口。完整实现由后续 agent 填充：
 *              xterm.js 底部面板、多 tab 管理、快捷键、拖拽调高。
 *
 * 当前为占位实现，仅满足构建脚本能找到入口文件。
 */

/** 插件名 */
export const name = 'dsh-oh-my-terminal';

/**
 * 挂载浏览器半组件（占位实现）
 * @returns 卸载函数
 */
export function apply(): () => void {
  // 占位：完整实现由后续 agent 填充
}
